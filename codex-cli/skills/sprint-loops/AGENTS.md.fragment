## Sprint Loops

Use `$sprint-loops` when the user explicitly invokes it or directly asks to
start, continue, resume, or run Sprint Loops. Its canonical Book v2 state lives
under `docs/`; intent chapters are semantic authority. The installed skill owns
routing and phase contracts, so keep this file as a pointer rather than a
second protocol.
