# Project-owned ignores (outside the sprint-loops marker so the scaffold never
# rewrites them). OS-junk files that predate the Book migration.
.DS_Store
Thumbs.db
# >>> sprint-loops >>>
# The Book is tracked. Ignore only transient helper output.
*.tmp
/guards-report.ndjson
# <<< sprint-loops <<<
