# Sprint 5 — Integration Tests

The 12-step `selftest.sh` continues to verify the script + bash layer end-to-end.

| Test | Result |
|------|--------|
| `test_install_then_selftest_12` — install via `claude-code/install.sh` and run installed selftest | **PASS** (both bundles report `all 12 transitions matched`) |

**Totals: 1 passed / 0 failed / 1 total** (× 2 bundles).
