# Agent Tasks (Persistent Backlog)

- [ ] T-101 (backlog): array-test engine integration — guards as content-addressed cells, memoized frontier runs, Merkle-root Loop gate; GATED on array-test T1–T5 shipping (see ROADMAP.md §1) — touches: tools/run-guards.sh, phases/05+06, schemas/test-report.md
- [ ] T-104 (backlog): plugin version field + per-sprint bump + documented /plugin update reload; skill prints bundle version at Init — touches: claude-code/.claude-plugin/plugin.json, READMEs, claude-code SKILL.md, scripts/init-sprint.sh
- [ ] T-105 (backlog): abort-sprint.sh no-git graceful fallback — touches: {4 bundles}/scripts/abort-sprint.sh, selftest.sh
- [ ] T-106 (backlog): antigravity bundle parity decision (grow translation layer vs document thin-adapter) — touches: antigravity-ide/global_workflows/sprint-loops.md, antigravity-ide/README.md
- [ ] T-107 (backlog): launch-time E2E harness for picker count + auto-trigger survival — touches: tools/, .github/workflows/ci.yml
- [ ] T-108 (backlog): record confidence before→after in sprint-meta at Loop close — touches: {4 bundles}/scripts/update-confidence.sh, phases/06, schemas/sprint-meta.md
- [ ] T-109 (backlog): dev-branch working model — at Init (sprint 0), establish/verify a long-lived work branch ("dev" or user-named) alongside main; sprints develop on it and each sprint's PR targets main; at launch, elicit approve-merge vs auto-merge unless the initial prompt already specified it (user request, s11) — touches: {4 bundles}/scripts/init-sprint.sh, phases/01+06, claude-code SKILL.md, schemas/sprint-meta.md
- [ ] T-121 (backlog): make finalize-plan.sh CRLF detection robust to `\r`-stripping awk — Windows/MSYS2 GNU awk strips trailing `\r`, so the `awk 'NR==1 {...substr($0,length($0),1)=="\r"...}'` checks at finalize-plan.sh lines 167/172 misdetect CRLF plans as LF and prepend an LF header, breaking uniform CRLF; use a byte-safe detection (e.g. `case "$(head -n1 "$f")" in *"$CR")`) and mirror the fix in the runtime-helpers CRLF assertions; passes on POSIX CI, fails only under Windows git-bash — touches: {4 bundles}/scripts/finalize-plan.sh, {4 bundles}/scripts/runtime-helpers.test.sh
- [ ] T-136 (backlog) [intent: INT-0003]: after the human-approved Sprint 16 `dev → main` merge and explicit deletion authority, verify `main` contains the v2 profile, Dependabot `dev` target, `dev` PR CI coverage, and Actions v7; resync `main → dev`; delete the local and remote retired dependency branch; then verify the remote long-lived branch set is exactly `main`/`dev` and no open PR names the retired branch — fulfills M-001 and realizes INT-0003
- [ ] T-143 (backlog) [intent: INT-0004]: widen convergence rollback coverage to the remaining DEPLOY_SUBSTRATE_FAIL_AFTER injection points (book, profile, updater) so "rolls back at any step" is proved rather than argued from subset reasoning (Sprint 17 test critique C-001) — touches: {4 bundles}/scripts/deploy-substrate.test.sh
- [ ] T-144 (backlog) [intent: INT-0004]: fix the guard runner's console summary reporting `det-mismatch` for any failing suite in a --determinism run — `${det:+ det-mismatch}` expands when the field is set, not when it mismatched; the ndjson confirmation is already correct (Sprint 17 test critique C-005) — touches: tools/run-guards.sh
- [ ] T-145 (backlog) [intent: INT-0004]: after the Sprint 17 checkpoint merges, run `/plugin update sprint-loop` and verify the installed Claude Code bundle is byte-identical to claude-code/skills/sprint-loop for scripts/ and phases/, closing test_installed_bundle_parity — touches: (verification only)
- [ ] T-152 (backlog) [intent: INT-0005]: wire check-tracked.sh into the checkpoint path, or reconcile INT-0005's Intent prose with its acceptance criteria — the chapter says the gate is wired there and the implementation does not do it; exposure is nil because close-sprint gates immediately before, but the chapter and the code disagree (Sprint 18 test critique C-002) — touches: {4 bundles}/scripts/remote-adapter.sh or docs/intents/INT-0005-turn-and-checkpoint-contract.md
- [ ] T-153 (backlog) [intent: INT-0005]: build the local pre-commit hook that refuses commits on the base branch, create-if-absent and never clobbering an existing hook or a core.hooksPath redirect — the remaining scoped-out part of INT-0005 (Sprint 18 research F7) — touches: {4 bundles}/scripts/deploy-substrate.sh, {4 bundles}/scripts/
- [ ] T-154 (backlog) [intent: INT-0004]: when a task changes a shared helper's observable output, require the plan's integration section to enumerate that helper's existing consumers — Sprint 18's worst defect broke deploy-substrate, a consumer from the previous sprint's intent, and no fixture written that sprint could have caught it (Sprint 18 test critique C-001) — touches: {4 bundles}/phases/03-plan-phase.md, prompts/plan-critic.md
- [ ] T-155 (backlog) [intent: INT-0005]: prioritize T-121 — it has now aborted the local guard suite before the sprint's own fixtures for two consecutive sprints, forcing bespoke harness verification each time (Sprint 18 test critique C-003) — touches: {4 bundles}/scripts/finalize-plan.sh
- [ ] T-156 (backlog) [intent: INT-0006]: reconcile existing projects' remote profiles — every project bootstrapped before provider detection lands carries whatever Sprint 0 defaulted to, which is `local-only` for any project converged without an explicit `--provider`; report the disagreement between the recorded provider and the current origin rather than auto-rewriting a field the operator may have set deliberately — touches: {4 bundles}/scripts/deploy-substrate.sh, {4 bundles}/scripts/check-substrate.sh
- [ ] T-161 (backlog) [intent: INT-0005]: audit the fixture suites for negative assertions that can pass without the operation running — a "did not change" check must be paired with proof the command completed, as test_existing_profile_untouched was after it passed for the wrong reason (Sprint 19 test critique C-001) — touches: {4 bundles}/scripts/*.test.sh, tools/*.test.sh
- [ ] T-162 (backlog) [intent: INT-0006]: add an inference fixture for a filesystem-path origin (a bare repo on disk), which resolves to generic and is currently unasserted (Sprint 19 test critique C-003) — touches: {4 bundles}/scripts/deploy-substrate.test.sh
- [ ] T-163 (backlog) [intent: INT-0004]: reduce canonical-runner wall time — the deploy-substrate suite alone now runs 24 full convergences and the whole runner roughly triples under machine contention, which is how leaning on CI becomes habitual; revisit alongside the ROADMAP memoization decision (Sprint 19 test critique C-004) — touches: tools/run-guards.sh
- [ ] T-165 (sprint 20) [intent: INT-0012]: generate the host's CI configuration from detected languages, refusing when the workflow directory is non-empty — touches: {4 bundles}/scripts/scaffold-ci.sh, {4 bundles}/scripts/scaffold-ci.test.sh, tools/check-bundle-sync.sh, tools/run-guards.sh
- [ ] T-166 (sprint 20) [intent: INT-0012]: wire CI generation into convergence and --check as a rollback-tracked step bound at contract 4 — touches: {4 bundles}/scripts/deploy-substrate.sh, {4 bundles}/scripts/deploy-substrate.test.sh
- [ ] T-167 (sprint 20) [intent: INT-0012]: document what convergence generates per host and how to opt out — touches: {claude-code,codex-cli}/skills/*/phases/01-init-sprint.md, open-harnesses/particles/01-init-sprint.md, antigravity-ide/global_workflows/sprint-loops.md, README.md, tools/operator-docs.test.sh
