# Agent Tasks (Persistent Backlog)

- [ ] T-101 (backlog): array-test engine integration — guards as content-addressed cells, memoized frontier runs, Merkle-root Loop gate; GATED on array-test T1–T5 shipping (see ROADMAP.md §1) — touches: tools/run-guards.sh, phases/05+06, schemas/test-report.md
- [ ] T-104 (backlog): plugin version field + per-sprint bump + documented /plugin update reload; skill prints bundle version at Init — touches: claude-code/.claude-plugin/plugin.json, READMEs, claude-code SKILL.md, scripts/init-sprint.sh
- [ ] T-105 (backlog): abort-sprint.sh no-git graceful fallback — touches: {4 bundles}/scripts/abort-sprint.sh, selftest.sh
- [ ] T-106 (backlog): antigravity bundle parity decision (grow translation layer vs document thin-adapter) — touches: antigravity-ide/global_workflows/sprint-loops.md, antigravity-ide/README.md
- [ ] T-107 (backlog): launch-time E2E harness for picker count + auto-trigger survival — touches: tools/, .github/workflows/ci.yml
- [ ] T-108 (backlog): record confidence before→after in sprint-meta at Loop close — touches: {4 bundles}/scripts/update-confidence.sh, phases/06, schemas/sprint-meta.md
- [ ] T-109 (backlog): dev-branch working model — at Init (sprint 0), establish/verify a long-lived work branch ("dev" or user-named) alongside main; sprints develop on it and each sprint's PR targets main; at launch, elicit approve-merge vs auto-merge unless the initial prompt already specified it (user request, s11) — touches: {4 bundles}/scripts/init-sprint.sh, phases/01+06, claude-code SKILL.md, schemas/sprint-meta.md
- [ ] T-121 (backlog): make finalize-plan.sh CRLF detection robust to `\r`-stripping awk — Windows/MSYS2 GNU awk strips trailing `\r`, so the `awk 'NR==1 {...substr($0,length($0),1)=="\r"...}'` checks at finalize-plan.sh lines 167/172 misdetect CRLF plans as LF and prepend an LF header, breaking uniform CRLF; use a byte-safe detection (e.g. `case "$(head -n1 "$f")" in *"$CR")`) and mirror the fix in the runtime-helpers CRLF assertions; passes on POSIX CI, fails only under Windows git-bash — touches: {4 bundles}/scripts/finalize-plan.sh, {4 bundles}/scripts/runtime-helpers.test.sh
- [ ] T-123 (sprint 15) [intent: INT-0002]: deterministic substrate check (complete/absent/partial) in front of routing — touches: {4 bundles}/scripts/check-substrate.sh + .test.sh
- [ ] T-124 (sprint 15) [intent: INT-0002]: idempotent Sprint 0 deploy (Book+branches+ledgers+profile+first sprint, transactional) — touches: {4 bundles}/scripts/deploy-substrate.sh + .test.sh
- [ ] T-125 (sprint 15) [intent: INT-0002]: provider adapters (gh/glab/generic) + one-PR/MR-per-sprint — touches: {4 bundles}/scripts/remote-adapter.sh + .test.sh
- [ ] T-126 (sprint 15) [intent: INT-0002]: boundary work-branch resync (base→work, dirty refusal, write-only-work) — touches: {4 bundles}/scripts/sync-work-branch.sh + .test.sh
- [ ] T-127 (sprint 15) [intent: INT-0002]: rewire init/loop/bootstrap phase docs + adapters to substrate gate + profile + human-approve checkpoint — touches: {claude,codex} phases 01/06 + bootstrap doc, {4 bundles} SKILL.md, antigravity workflow, oh particles
- [ ] T-128 (sprint 15) [intent: INT-0002]: register substrate suites in run-guards + bundle-sync inventory + adapter-semantics + ×4 propagation — touches: tools/{run-guards,check-bundle-sync,check-adapter-semantics}.sh, {4 bundles}/scripts/*
- [ ] T-129 (sprint 15) [intent: INT-0002]: dogfood — retrofit this repo (dev/bump branches + remote-profile) to substrate-complete — touches: docs/work/remote-profile.md, repo branches, .gitignore
