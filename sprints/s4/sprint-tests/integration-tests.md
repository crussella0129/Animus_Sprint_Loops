# Sprint 4 — Integration Tests

The 12-step `selftest.sh` (now including the Decisions-Reviewed gate) is the
integration harness.

| Test | Result |
|------|--------|
| `test_install_then_selftest_12` — install via `claude-code/install.sh` and run the installed selftest | **PASS** (both bundles report `all 12 transitions matched`) |

**Totals: 1 passed / 0 failed / 1 total** (× 2 bundles, both green).
