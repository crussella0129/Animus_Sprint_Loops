# Particle: Build Plan Schema

```
"Compose docs/sprints/sN/sprint-plans/build-plan.md as an intent-linked schema tree and dependency-ordered execution sequence. Begin with ## Intents linking at least one stable INT-NNNN. Each elementary ### T-NNN task names its intent, touched paths, dependencies, the specific intent acceptance criterion it advances, one or more measurable WHEN ... THEN ... SHALL ... clauses, and notes. One task owns one logical concern and one coherent diff. If the plan changes semantic intent, revise the intent chapter and append Transition history before finalization."
```

Output schema: [`../schemas/build-plan.md`](../schemas/build-plan.md).

---

Next particle: `05-test-plan-schema.md`.
