# Particle: Research Phase

```
"Research a bounded sprint goal and create or select at least one docs/intents/INT-NNNN-*.md chapter. Treat that chapter as semantic authority and add a docs/SUMMARY.md navigation link for each new chapter without storing state in navigation. Write docs/sprints/sN/sprint-research/research-report.md with the exact ## Intents Reviewed heading, links to every relevant intent, the five required numbered sections, and references to all saved artifacts. If findings change outcome, acceptance, rationale, alternatives, consequences, or lifecycle state, update the stable intent and append Transition history; do not leave the change only in sprint provenance. Observe the 20-file, 5-source, and 30-minute-equivalent budget. Invoke the installed bundle's scripts/research-budget.sh helper from the project root; use a non-empty ## Budget Override only for justified cross-cutting breadth. Then invoke current-phase.sh and require plan."
```

Schema: [`../schemas/research-report.md`](../schemas/research-report.md).
Helpers: [`../scripts/research-budget.sh`](../scripts/research-budget.sh) and
[`../scripts/current-phase.sh`](../scripts/current-phase.sh).

---

Next particle: `03-plan-phase.md`.
